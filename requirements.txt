# Analytics core + data generation. Frontend deps are added in later steps.
numpy
pandas
SQLAlchemy
psycopg2-binary
pytest
# Reasoning layer (LangGraph nodes call Claude via the Anthropic SDK).
anthropic
python-dotenv
# LangGraph state machine + FastAPI server.
langgraph
fastapi
uvicorn
httpx
